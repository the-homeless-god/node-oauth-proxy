import { FULL_URL, PORT } from "./environment";

export const NOT_AUTH = `Navigate to ${FULL_URL}`;
export const NOT_ACCESS = "Access denied";

export const SERVER_HAS_BEEN_HOSTED = `Proxy server has been hosted on ${PORT} port`;
